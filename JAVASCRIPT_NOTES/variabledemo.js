// In JavaScript, variables are named containers used to store data.

/* In JavaScript there are 3 ways to create a variable
    1] using var keyword
    
    Syntax :- var <variable_name> = <value>;

    Eg :- var marks=90;

    2] using let keyword

    Syntax :- let <varable_name> = <value>;

    Eg:- let age = 10;

    3] usng const keyword (const here is constant)

    Syntax :- const <variable_name> = <value>;

    Eg :- const x = 100;
*/

/* Variable Namng Rules in JS

The following are the rules for naming variables in JavaScript:

-> Spaces are not allowed in variable names.
-> Only letters, digits, underscores, and dollar signs are permitted in variable names.
-> Case matters when it comes to variable names.
-> A letter (alphabet), an underscore (_), or a dollar sign ($) must be the first character in a variable name, any other special  characters must not be taken.
-> certain terms such as reserved words in javascript shouldn't be used to name variables.

*/

var text = "Learning about Variables";
console.log(text);

const employee_name = "Shourya";
console.log(employee_name);

let age = 23;
console.log(age);

var education = "MCA";
console.log(education);

let job = "web-developer";
console.log(job);

const employee_id = 102;
console.log(employee_id);
